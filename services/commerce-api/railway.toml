[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
# When this directory is Railway Root Directory, Dockerfile COPYs are local.

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
